<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <!-- Brand Logo -->
        <a class="navbar-brand" href="/">MyApp</a>

        <!-- Toggler for Mobile View -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navbar Links -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <!-- User Panel -->
                <li class="nav-item">
                    <a class="nav-link active" href="/">User Form</a>
                </li>
                <!-- Admin Panel -->
                <li class="nav-item">
                    <a class="nav-link" href="/admin">Admin Panel</a>
                </li>
            </ul>

            <!-- Login/Logout Buttons -->
            <ul class="navbar-nav">
                <!-- Login -->
                <% if (typeof userName === 'undefined') { %>
                    <li class="nav-item">
                        <a class="btn btn-outline-light me-2" href="/admin/login">Login</a>
                    </li>
                <% } %>

                <!-- Logout -->
                <% if (typeof userName !== 'undefined') { %>
                    <li class="nav-item">
                        <a class="btn btn-outline-light" href="/admin/logout">Logout</a>
                    </li>
                <% } %>

                <!-- Admin Signup (only if no admins are registered) -->
                <% if (RegisterAdminlength && RegisterAdminlength.length === 0) { %>
                    <li class="nav-item">
                        <a class="btn btn-outline-light" href="/admin/signup">SignUp</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
